concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ startsWith(github.ref, 'refs/pull/') }}
name: The CI workflow for feature branches and pull requests
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - '**'
      - '!main'
permissions:
  contents: read
run-name: >-
  ${{ github.event_name == 'pull_request' && format('Verified the pull request #{0} by @{1}', github.event.pull_request.number, github.actor) || format('Pushed the feature branch by @{0}', github.actor) }}
jobs:
  build:
    defaults:
      run:
        shell: ${{ matrix.platform.shell }}
    env:
      COREPACK_INTEGRITY_KEYS: 0
    name: The build process
    runs-on: ${{ matrix.platform.os }}
    steps:
      - name: Set git to use LF
        run: |
          git config --global core.autocrlf false
          git config --global core.eol lf
      - name: Stages the pushed branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Setup the pnpm
        uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9
        with:
          run_install: false
      - name: Prepare the Node.js version ${{ matrix.node-version }} environment
        uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          cache: pnpm
          check-latest: true
          node-version: ${{ matrix.node-version }}
      - env:
          HUSKY: 0
        name: Install the dependencies
        run: pnpm install --frozen-lockfile --prefer-offline
      - name: Build the project
        run: pnpm run build
      - name: Check the code style
        run: pnpm run lint
      - name: Run the tests
        run: pnpm run test
    strategy:
      matrix:
        node-version:
          - 20.x
          - 22.x
          - 24.x
        platform:
          - os: ubuntu-latest
            shell: bash
          - os: windows-latest
            shell: bash
          - os: windows-latest
            shell: powershell
      fail-fast: false
    timeout-minutes: 30
